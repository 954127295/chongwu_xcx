<map id="map" longitude="{{longitude}}" latitude="{{latitude}}" scale="14" markers="{{markers}}" show-location style="width: 100%; height: 300px;"></map>
剩余电量：{{battery}}

gps开关：
<view class="body-view">
    <switch checked="{{gps}}" bindchange="gpsChange"/>
</view>